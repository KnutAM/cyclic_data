data_path = ("This string can contain jibberish because setup.py will overwrite this file that is not version controlled" + 
             "However, this file and the definition of data_path is required for the Travis CI test runs where setup.py is not used")
